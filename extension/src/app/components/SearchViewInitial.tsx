function SearchViewInitial() {
  return (
    <div className="col-span-2 flex flex-col items-center justify-center">
      <div className="text-xs text-muted-foreground">
        Use the text field above to do a semantic search among the documents.
      </div>
    </div>
  );
}

export default SearchViewInitial;
